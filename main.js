var config={
    width: "100",
    height: "100",
    physics: {
        default: "arcade",
        arcade: {
            debug: false
        }
    },
    scene: [Scene1,Scene2]
}
var game = new Phaser.Game(config);
